services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_RENTAL_REGISTRY_ADDRESS: ${NEXT_PUBLIC_RENTAL_REGISTRY_ADDRESS:-}
      NEXT_PUBLIC_CHAIN_ID: ${NEXT_PUBLIC_CHAIN_ID:-11155111}
